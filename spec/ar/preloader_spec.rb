require 'spec_helper'

describe Ar::Preloader do
  it 'has a version number' do
    expect(Ar::Preloader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
